Load the driver using insmod chardev.ko

With appropriate privileges and command-line arguments, run the application "application":

    ./application — to display the driver variables
    ./application -c — to clear the driver variables
    ./application -g — to display the driver variables
    ./application -s — to set the driver variables (not mentioned above)

Unload the driver using rmmod chardev